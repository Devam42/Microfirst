# ============================================================================
# Microbot Environment Configuration
# ============================================================================
# IMPORTANT: Copy this file to .env and fill in your actual credentials
# Command: cp env.example .env (Linux/Mac) or copy env.example .env (Windows)
# 
# ⚠️  SECURITY WARNING ⚠️
# - NEVER commit .env file to Git (already in .gitignore)
# - Keep your credentials secure and private
# - Rotate credentials regularly for security
# ============================================================================

# ----------------------------------------------------------------------------
# AWS Credentials (Required for Voice/TTS features)
# ----------------------------------------------------------------------------
# Get these from: https://console.aws.amazon.com/iam/
# You need AWS Polly access for text-to-speech functionality
#
# Alternative env var names also supported:
#   - MICROBOT_AWS_ACCESS_KEY (instead of AWS_ACCESS_KEY_ID)
#   - MICROBOT_AWS_SECRET_KEY (instead of AWS_SECRET_ACCESS_KEY)

AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_DEFAULT_REGION=ap-south-1

# Recommended regions:
#   - ap-south-1 (India - Mumbai)
#   - us-east-1 (US East - N. Virginia) 
#   - eu-west-1 (Europe - Ireland)

# ----------------------------------------------------------------------------
# Google Gemini API Key (Required for AI Chat)
# ----------------------------------------------------------------------------
# Get your API key from: https://makersuite.google.com/app/apikey
# This is required for all AI conversation features
#
# Alternative env var names also supported:
#   - GEMINI_API_KEY (instead of GOOGLE_API_KEY)

GOOGLE_API_KEY=your_google_gemini_api_key_here

# ----------------------------------------------------------------------------
# Application Settings (Optional)
# ----------------------------------------------------------------------------
# These have sensible defaults but can be customized

# Default language for bot responses
# Options: english, hinglish, marathi
DEFAULT_LANGUAGE=hinglish

# Display name of your bot
BOT_NAME=Microbot

# Server port (default: 5000)
PORT=5000

# Python environment (development/production)
# PYTHONUNBUFFERED=1

# ----------------------------------------------------------------------------
# Optional: Future Database Settings
# ----------------------------------------------------------------------------
# Uncomment when you add database support
# DATABASE_URL=postgresql://user:password@localhost:5432/microbot

# ----------------------------------------------------------------------------
# Optional: Logging & Debug
# ----------------------------------------------------------------------------
# LOG_LEVEL=INFO
# DEBUG=False


